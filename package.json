{
  "name": "launchops",
  "version": "0.1.0",
  "private": true,
  "description": "LaunchOps — Play Store testing platform (client / tester / admin)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web",
    "dev:api": "npm run dev --workspace apps/api",
    "build": "npm run build -w @launchops/types && npm run build -w @launchops/api && npm run build -w @launchops/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
